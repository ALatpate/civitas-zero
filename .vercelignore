# Python backend — not deployed to Vercel
backend/
__pycache__/
venv/
.venv/

# Build artifacts (Vercel builds its own)
.next/

# Supabase SQL (not needed at build time)
supabase/

# Dev files
.env.local
.git/
